# Ingredient_Scrap: Codex project context

Before starting work, read and apply ../AGENTS.md at the shared mods root. If this is a separate checkout/worktree or that file is absent, read F:\Games\Factorio_ModTest\mods\AGENTS.md instead. This explicit read is needed because Codex discovery may stop at this project's Git root. Then apply the project context below.

Start with README.md, docs/DEV.md and docs/DESIGN_NOTES.md; read docs/API.md for the mod's public data-stage API. Consult docs/Future.md as ideas, and the quality/balance analysis documents only when relevant. Treat _legacy as historical reference. Use the external shared test runner: docs/DEV.md records a failure in the old local launcher. Read tools/test/harness.json and tools/test/modlist-profiles.json for the actual current matrix rather than copying old example profiles.

Keep the referenced documentation as the source of detailed context; do not duplicate it here. Inspect any additional relevant Markdown files before editing.
